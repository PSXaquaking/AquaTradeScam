local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/shlexware/Rayfield/main/source'))()

local Window = Rayfield:CreateWindow({
    Name = "AquaSkam",
    LoadingTitle = "AquaSkam loading",
    LoadingSubtitle = "by Aquaking",
    ConfigurationSaving = {
       Enabled = true,
       FolderName = AquaSkam, -- Create a custom folder for your hub/game
       FileName = "AquaSkam"
    },
    Discord = {
       Enabled = false,
       Invite = "NjVsm9VZRa", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ABCD would be ABCD.
       RememberJoins = true -- Set this to false to make them join the discord every time they load it up
    },
    KeySystem = true, -- Set this to true to use our key system
    KeySettings = {
       Title = "AquakingSkamHub",
       Subtitle = "Key System",
       Note = "Join the discord (discord.gg/NjVsm9VZRa)",
       FileName = "AquakingKey",
       SaveKey = true,
       GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
       Key = "AquakingW"
    }
 })

  local Tab = Window:CreateTab("Main", 4483362458)
  local Section = Tab:CreateSection("Main")

  local Button = Tab:CreateButton({
    Name = "Enable Scam",
    Callback = function()
   _G.Key = "gnikauqa_rls" 
   loadstring(game:HttpGet("https://raw.githubusercontent.com/psx-Scripts/psxsc/main/script-v4-1.lua"))()
    end,
 })

  local Slider = Tab:CreateSlider({
   Name = "number of pets for scam ",
   Range = {0, 100},
   Increment = 10,
   Suffix = "Pets",
   CurrentValue = 10,
   Flag = "Slider1", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value)
   -- The function that takes place when the slider changes
   -- The variable (Value) is a number which correlates to the value the slider is currently at
   end,
})

  local Slider = Tab:CreateSlider({
   Name = "Walkspeed ",
   Range = {0, 100},
   Increment = 1,
   Suffix = "Pets",
   CurrentValue = 50,
   Flag = "Slider1", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value)
   game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 50
   end,
})

local Tab = Window:CreateTab("Booth", 4483362458)
local Section = Tab:CreateSection("Booth Scam")

local Button = Tab:CreateButton({
  Name = "booth scam",
  Callback = function()
Rayfield:Notify({
   Title = "discord.gg/NjVsm9VZRa",
   Content = "Now, choose a fake price",
   Duration = 6.5,
   Image = 4483362458,
   Actions = { -- Notification Buttons
      Ignore = {
         Name = "Okay!",
         Callback = function()
         print("Now choose a fake price!")
      end
   },
},
})
   end,
})

local Slider = Tab:CreateSlider({
   Name = "Scam booth price ",
   Range = {0, 1000000},
   Increment = 1000,
   Suffix = "Gems",
   CurrentValue = 1,
   Flag = "Slider1", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value)
   -- The function that takes place when the slider changes
   -- The variable (Value) is a number which correlates to the value the slider is currently at
   end,
})
